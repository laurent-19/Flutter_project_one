//import 'package:http/http.dart';

Future<void> main() async {
  // final Response response = await get('https://yts.mx/api/v2/list_movies.json');//final Map<String, dynamic> map = jsonDecode(response.body) as Map<String, dynamic>;
  //final List<dynamic> movieList = map['data']['movies'];
  //for(int i = 0; i< movieList.length; i++)
  // print(movieList[i]['title']);
}

/*class Movie {
  final String title;
  final int year;
  final String summary;
  final List<String> genders;

  Movie(this.title, this.year, this.summary, this.genders);
}*/
